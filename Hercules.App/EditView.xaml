<controls:MindmapFlyoutPopupView x:Class="Hercules.App.EditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Hercules.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Uid="SettingsHeader" FontSize="24" FontWeight="Light" Margin="0,0,0,10">Mindmap Settings</TextBlock>

        <StackPanel Grid.Row="1" Margin="0, 10">
            <TextBlock x:Uid="MindmapTitleError" Text="You cannot leave the name blank!" Foreground="Red" Visibility="Collapsed" x:Name="ErrorTextBlock" />
            <TextBlock x:Uid="MindmapTitleLabel" Text="Mindmap Title:" Margin="0,0,0,4" />

            <TextBox Text="{Binding Name, Mode=TwoWay}" x:Name="NameTextBox" TextChanged="NameTextBox_TextChanged" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button x:Name="DeleteMindmapButton" x:Uid="DeleteMindmapButton" Content="Delete Mindmap" Click="DeleteMindmapButton_Click" />
        </StackPanel>
    </Grid>
</controls:MindmapFlyoutPopupView>
