<UserControl
    x:Class="Hercules.App.EnterNameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hercules.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="270" MinHeight="125">
    
    <Border Padding="10" BorderBrush="{StaticResource PopupBorderBrush}" BorderThickness="2" Background="{StaticResource PopupBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <StackPanel>
                <TextBlock x:Uid="NewMindmapNameError" Text="You cannot leave the name blank!" Foreground="Red" Visibility="Collapsed" x:Name="ErrorTextBlock" />
                <TextBlock x:Uid="NewMindmapNameLabel" Text="Enter Name:" Margin="0,0,0,4" />
                <TextBox x:Name="NameTextBox" />
            </StackPanel>

            <Button Margin="-3,10,0,0" Grid.Row="1" Content="Create" Click="CreateButton_Click" x:Uid="CreateButton" />  
        </Grid>
    </Border>
</UserControl>
