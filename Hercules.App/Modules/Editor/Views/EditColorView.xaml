<controls:MindmapFlyoutView x:Class="Hercules.App.Modules.Editor.Views.EditColorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Hercules.App.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Width="310">

    <controls:MindmapFlyoutView.Resources>
        <controls:ThemeBrushConverter x:Key="ThemeBrushConverter" />
    </controls:MindmapFlyoutView.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Uid="Editor_ColorsHeader" FontSize="24" FontWeight="Light" Margin="0,0,0,10" Text="Colors" />

        <GridView VerticalAlignment="Top" Grid.Row="1" Margin="0,10" x:Name="ColorsGrid" Padding="0">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding Converter={StaticResource ThemeBrushConverter}}" Width="32" Height="32" />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <ToggleButton x:Uid="Editor_ShowHullButton" Grid.Row="2" Margin="0,20,0,0" Content="Show Hull" x:Name="ShowHullButton" Click="ShowHullButton_Click" />
    </Grid>
</controls:MindmapFlyoutView>
