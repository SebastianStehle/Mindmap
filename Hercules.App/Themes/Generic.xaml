<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hercules.App.Controls"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:gpi="using:GP.Windows.UI.Interactivity"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:controls="using:GP.Windows.UI.Controls">

    <Style TargetType="local:Mindmap">
        <Setter Property="i:Interaction.Behaviors">
            <Setter.Value>
                <i:BehaviorCollection>
                    <local:NodeMovingBehavior />
                </i:BehaviorCollection>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:Mindmap">
                    <Grid>
                        <controls:ZoomableCanvasControl x:Name="PART_Canvas" Width="{Binding Document.Width}" Height="{Binding Document.Height}"  />

                        <local:TextEditor x:Name="PART_TextBox" Style="{StaticResource NoClearButtonTextBox}" Padding="0"
                            MinHeight="0"
                            MinWidth="0"
                            TextAlignment="Center" 
                            Background="Transparent" 
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            Visibility="Visible" 
                            VerticalContentAlignment="Center"
                            VerticalAlignment="Top"
                            HorizontalContentAlignment="Center"
                            HorizontalAlignment="Left">
                        </local:TextEditor>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
